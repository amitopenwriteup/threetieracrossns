---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: wb-pvc
  namespace: wordpress
spec:
  storageClassName: nfs-client
  resources:
    requests:
      storage: 10Gi
  accessModes:
    - ReadWriteOnce

